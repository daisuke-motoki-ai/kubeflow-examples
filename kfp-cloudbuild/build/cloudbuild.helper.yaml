steps:


- name: 'gcr.io/cloud-builders/gcloud'
  args: [
      'builds', 
      'submit',
      '--config=kfp-cloudbuild/build/cloudbuild.yaml',
      '--substitutions=${SUBSTITUTIONS}',
      '.'
  ]
